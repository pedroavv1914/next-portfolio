export default function Contato() {
  return (
    <section className="formulario" id="formulario">
      <div className="interface">
        <h2 className="titulo">FALA <span>COMIGO.</span></h2>
        {/* Formulário com validação será migrado em etapa seguinte */}
      </div>
    </section>
  );
}
